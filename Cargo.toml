[workspace]
resolver = "2"
members = [
    "crates/core",
    "crates/cli",
    "crates/mcp",
]

# workspace 根定义 profile（成员 crate 内的 profile 会被忽略）
[profile.release]
opt-level = 3
lto = true
strip = true

[workspace.lints.rust]
warnings = "deny"
